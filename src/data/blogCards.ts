export interface ICardData {
  title: string;
  slug: string;
  description: string;
  published: boolean;
}

const blogCardsData: ICardData[] = [
  {
    title: 'hello',
    slug: 'htl',
    description: 'jhv kjh kh ',
    published: false,
  },
];

export default blogCardsData;
